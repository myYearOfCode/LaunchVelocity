class graph
    def initialize (graph)
        @dirtyGraph = dirtyGraph
        @graph = []
    end
    # this is a data structure consisting of days (date:commits key:val pair)
    # do we pass in the svg and then parse it in here? 
    # with a print method

    # we will need an accessor method for specific dates. 
    # we could also build calculating the stats into this class.


end